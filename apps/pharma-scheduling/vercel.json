{
  "installCommand": "cd ../.. && npm install && npm run build:auth && npm run build:ui",
  "buildCommand": "npm run build",
  "outputDirectory": ".next",
  "framework": "nextjs",
  "env": {
    "NODE_ENV": "development"
  }
}