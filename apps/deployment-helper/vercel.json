{
  "installCommand": "cd ../.. && npm install && npm run build:auth && npm run build:cache && npm run build:config && npm run build:db && npm run build:integrations && npm run build:monitoring && npm run build:types && npm run build:ui && npm run build:utils",
  "buildCommand": "npm run build",
  "outputDirectory": ".next",
  "framework": "nextjs",
  "env": {
    "NODE_ENV": "development"
  }
}